s="<big><big>";e="</big></big>"
kdialog --geometry 1000x900+400+10 --imgbox Intro7.jpg
#ajouter a fstab :: /dev/sda1 /m ntfs user,nofail,ro

while true;do
sleep 2 #if test -e /dev/sdb1 ??
if test -e /dev/sda1 -a -e SD_UNMOUNTED;then
sudo mount /dev/sda1 /m
CHKU=`ps -ef |grep zenity|wc -l`
if test $CHKU -eq 1;then echo -n U >> /home/marco/zenit;/home/marco/U
fi

else
sudo umount /m
if test -e /dev/sda1;then echo
else >SD_UNMOUNTED
fi
fi
done
